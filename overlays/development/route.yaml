apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend
  lables:
    app: todonodejs
    name: frontend
spec:
  host: todo.apps.ocp4.example.com
  path: /todo
  to:
    kind: Service
    name: dev-frontend

